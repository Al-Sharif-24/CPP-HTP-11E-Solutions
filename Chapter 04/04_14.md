# Exercise 4.14: Whats the Output of the Code

**What does each of the following statements print?**

- a)
```cpp
    cout << (i == 1) << '\n';
```
```txt
1
```

- b)
```cpp
    cout << (j == 3) << '\n';
```
```txt
0
```

- c)
```cpp
    cout << (i >= 1 && j < 4) << '\n';
```
```txt
1
```

- d)
```cpp
    cout << (m <= 99 && k < m) << '\n';
```
```txt
0
```

- e)
```cpp
    cout << (i == 1) << '\n';cout << (j >= i || k == m) << '\n';
```
```txt
1
```

- f)
```cpp
    cout << (k + m < j || 3 - j >= k) << '\n';
```
```txt
0
```

- g)
```cpp
    cout << (!m ) << '\n';
```
```txt
0
```

- h)
```cpp
    cout << (!( j - m)) << '\n';
```
```txt
1
```

- i)
```cpp
    cout << (!(k > m)) << '\n';
```
```txt
0
```